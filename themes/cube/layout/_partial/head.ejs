<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <meta name="author" content="<%= config.author %>">
    <% if (config.description) { %>
    <meta name="description" content="<%= config.description %>">
    <% } %>
    <% let title = [config.title] %>
    <% if (page.tag) {
        title.unshift(`${__('tags')}: ${page.tag}`)
    } %>
    <% if (page.category) {
        title.unshift(`${__('categories')}: ${page.category}`)
    }%>
    <% if (page.archive) {
        title.unshift(`${__('archive_a')}`)
    }%>
    <% if (page.title) {
        title.unshift(`${page.title}`)
    }%>
    <title><%= title.join(' | ') %></title>
    <!-- inject:style -->
    <link href="<%- url_for('css/style.css') %>" rel="stylesheet" type="text/css">
    <!-- endinject -->
    <style>
        .cube-loading {
            top: 0;
            position: fixed;
            width: 100%;
            height: 100%;
            background: url('<%- url_for('images/lg/loading.gif') %>') no-repeat center center;
            background-color: rgba(0,0,0,.7);
        }

        .cube-loading.out {
            display: none;
        }

        .cube-loading:before {
            display: block;
            content: '<%= __('loading') %>';
            position: relative;
            width: 100%;
            top: 50%;
            right: -50%;
            color: #fff;
        }

        @media(max-width: 768px) {
            .cube-loading:before {
                font-size: 1.2em;
                transform: translate(-24px,20px);
                -webkit-transform: translate(-24px,20px);
                -o-transform: translate(-24px,20px);
                -ms-transform: translate(-24px,20px);
            }
        }

        @media(min-width: 768px) {
            .cube-loading:before {

            }
        }

        .cube-type .cube-header-subtitle {
            display: block;
            margin-top: 12px;
            color: rgba(255, 255, 255, .9);
            font-size: 24px;
            line-height: 1.35;
            letter-spacing: 0;
            text-shadow: 0 2px 12px rgba(0, 0, 0, .35);
        }

        .cube-home-intro {
            margin-bottom: 20px;
            padding: 34px 28px;
            background: #fff;
            box-shadow: 0 2px 2px 0 rgba(0, 0, 0, .14), 0 3px 1px -2px rgba(0, 0, 0, .2), 0 1px 5px 0 rgba(0, 0, 0, .12);
            border-radius: 5px;
        }

        .cube-home-intro p {
            margin: 0;
            color: #555;
            font-size: 22px;
            line-height: 1.65;
            text-align: center;
        }

        @media(min-width: 768px) {
            .cube-content.no-sidebar {
                justify-content: center;
            }

            .cube-content.no-sidebar .cube-left {
                flex-basis: 78%;
                max-width: 860px;
            }
        }

        @media(max-width: 768px) {
            .cube-type .cube-header-subtitle {
                margin-top: 8px;
                font-size: 17px;
            }

            .cube-home-intro {
                margin: 0;
                padding: 26px 20px;
                box-shadow: none;
                border-radius: 0;
            }

            .cube-home-intro p {
                font-size: 18px;
            }
        }

        <% if (theme.opening_animation !== false) {
            let openingBackground = theme.background || (theme.header && theme.header.background)
            let openingIsUrl = /^(https?:)?\/\//.test(openingBackground || '')
            let openingUrl = openingBackground ? (openingIsUrl || openingBackground.charAt(0) === '/' ? openingBackground : url_for(`images/${openingBackground}`)) : ''
        %>
        .site-opening {
            position: fixed;
            inset: 0;
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            pointer-events: none;
            background-color: #111;
            transform: translateY(0);
            animation: site-opening-exit 1s cubic-bezier(.76, 0, .24, 1) 1.45s forwards;
            will-change: transform;
        }

        .site-opening::before {
            content: '';
            position: absolute;
            inset: -2%;
            background-image: linear-gradient(rgba(0, 0, 0, .28), rgba(0, 0, 0, .38))<% if (openingUrl) { %>, url('<%- openingUrl %>')<% } %>;
            background-position: center;
            background-size: cover;
            transform: scale(1.08);
            animation: site-opening-zoom 2.1s ease forwards;
        }

        .site-opening::after {
            content: '';
            position: absolute;
            left: 0;
            right: 0;
            bottom: -22px;
            height: 44px;
            background: linear-gradient(to bottom, rgba(0, 0, 0, .32), rgba(0, 0, 0, 0));
        }

        .site-opening-copy {
            position: relative;
            text-align: center;
            animation: site-opening-title 1.2s ease forwards;
        }

        .site-opening-title {
            color: #fff;
            font-size: 36px;
            line-height: 1.25;
            text-shadow: 0 2px 14px rgba(0, 0, 0, .45);
        }

        .site-opening-subtitle {
            margin-top: 12px;
            color: rgba(255, 255, 255, .86);
            font-size: 20px;
            line-height: 1.35;
            text-shadow: 0 2px 12px rgba(0, 0, 0, .45);
        }

        @keyframes site-opening-zoom {
            from { transform: scale(1.12); }
            to { transform: scale(1); }
        }

        @keyframes site-opening-title {
            0% {
                opacity: 0;
                transform: translateY(18px);
            }
            35%, 75% {
                opacity: 1;
                transform: translateY(0);
            }
            100% {
                opacity: 0;
                transform: translateY(-12px);
            }
        }

        @keyframes site-opening-exit {
            from {
                transform: translateY(0);
            }
            to {
                transform: translateY(-110%);
                visibility: hidden;
            }
        }

        @media(max-width: 768px) {
            .site-opening-title {
                font-size: 26px;
                max-width: 86%;
            }

            .site-opening-subtitle {
                font-size: 16px;
            }
        }

        @media(prefers-reduced-motion: reduce) {
            .site-opening {
                display: none;
            }
        }
        <% } %>
    </style>
    <% if (theme.analytics.baidu) { %>
    <%- partial('analytics/baidu') %>
    <% } else if (theme.analytics.google) { %>
    <%- partial('analytics/google') %>
    <% } %>
</head>
